# No LFS tracking for any files
*.glb -filter -diff -merge -text

# Previous rules (already set, keeping for consistency)
Assets/GameObjects/Resourses/** -filter -diff -merge -text
*.json -filter -diff -merge -text
# Optional: Handle scripts and metadata
*.cs diff merge text
*.meta diff merge text
*.glb !text !filter !merge !diff
